import { TrackOptions } from '../../_STORE/tracklist/action'
import { Dispatch } from 'redux'

export const mapDispatchToProps = (dispatch: Dispatch) => ({

})

//ADD TRACK
export const addTrack = (track:string) => {}